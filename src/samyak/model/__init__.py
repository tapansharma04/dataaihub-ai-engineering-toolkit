"""Model Intelligence domain (internal).

This package is the 0.2.0 catalog foundation. It is not part of the public
``samyak`` API yet: there is no load/query/update surface, so exporting types
from ``samyak.__init__`` would freeze a premature contract.

Import from ``samyak.model.*`` inside the product and tests only.
"""
