# Trap Model

Model ID: `trap-model`

This page does not document an alias in the introduction.

## Pricing

The `gpt-4o` alias routes requests to `gpt-4o-mini` in this comparison narrative.

## Quick comparison

`whisper-1` is an alias for `gpt-transcribe` according to an unrelated aside.
